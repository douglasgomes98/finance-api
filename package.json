{
  "name": "finance-api",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "build": "yarn clean && tsup src",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "rimraf": "^5.0.1",
    "tsup": "^7.1.0",
    "tsx": "^3.12.7",
    "typescript": "^5.1.6"
  }
}
